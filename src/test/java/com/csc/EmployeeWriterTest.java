package com.csc;

import com.csc.model.Employee;
import org.junit.Before;
import org.junit.Test;

import java.sql.SQLException;

public class EmployeeWriterTest {
/*
  SqlDatabase db;
  EmployeeWriter writer;

  @Before
  public void setup() throws SQLException {
    db = new SqlDatabase(BuildConfig.MY_SQL_URL, BuildConfig.MY_SQL_DB, BuildConfig.MY_SQL_USER,
        BuildConfig.MY_SQL_PS);
    writer = new EmployeeWriter(db);
  }

  @Test
  public void insertEmployee() throws Exception {
    Employee tmpEmployee = new Employee("Austin", "Ray", "test",
        "pass", 1, "PLANE");

    writer.insertEmployee(tmpEmployee);
  }
*/
}